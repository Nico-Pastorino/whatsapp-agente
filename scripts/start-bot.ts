/**
 * start-bot.ts — alias de start-worker.ts (multi-tenant).
 *
 * Este archivo existe por compatibilidad con el script `npm run start:bot`.
 * El worker real es start-worker.ts.
 */

import "./start-worker";
